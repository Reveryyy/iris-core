[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"


[project]
name = "iris-core"
version = "0.1.0"
description = "Core dell'assistente personale IRIS"
requires-python = ">=3.12"


[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]